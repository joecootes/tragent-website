{
  "name": "tragent-landing",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r index.html privacy.html terms.html styles.css script.js assets dist/"
  }
}
